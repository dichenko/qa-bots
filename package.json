{
  "name": "qa-bots",
  "version": "1.0.0",
  "description": "Telegram Bot for Q&A",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod",
    "deploy:with-webhooks": "vercel deploy --prod && npm run setup-webhooks",
    "setup-webhooks": "curl -X GET $(vercel --prod --confirm url)/api/setWebhook"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.39.3",
    "telegraf": "^4.16.0"
  },
  "devDependencies": {
    "vercel": "^33.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 